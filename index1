<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Your Name — Flowing Water Portfolio</title>
  <meta name="description" content="Personal website of Your Name — serene, water-inspired portfolio." />

  <style>
    :root {
      --water-light: #cce7ff;
      --water-mid: #4da6ff;
      --water-dark: #0059b3;
      --text-dark: #042a4f;
      --text-light: #eaf6ff;
      --bg: #eaf6ff;
      --wave-height: 120px;
      font-family: "Segoe UI", Roboto, system-ui, sans-serif;
    }

    body {
      margin: 0;
      background: linear-gradient(180deg, var(--water-light) 0%, var(--bg) 100%);
      color: var(--text-dark);
      line-height: 1.6;
      overflow-x: hidden;
    }

    header {
      text-align: center;
      padding: 80px 20px 100px;
      background: linear-gradient(180deg, var(--water-mid), var(--water-light));
      color: var(--text-light);
      position: relative;
    }

    header h1 {
      font-size: 2.2rem;
      margin: 0;
    }

    header p {
      font-size: 1.1rem;
      margin-top: 10px;
      opacity: 0.9;
    }

    nav {
      margin-top: 20px;
    }

    nav a {
      color: var(--text-light);
      text-decoration: none;
      margin: 0 12px;
      font-weight: 600;
      transition: opacity 0.3s ease;
    }

    nav a:hover {
      opacity: 0.75;
    }

    /* Wave animation */
    .wave {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 200%;
      height: var(--wave-height);
      background: url('https://raw.githubusercontent.com/konpa/devicon/master/icons/wave/wave.svg');
      opacity: 0.3;
      animation: waveMove 8s linear infinite;
      transform: translate3d(0, 0, 0);
    }

    @keyframes waveMove {
      from { transform: translateX(0); }
      to { transform: translateX(-50%); }
    }

    main {
      max-width: 900px;
      margin: -60px auto 40px;
      background: white;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
      padding: 30px 24px;
      position: relative;
      z-index: 2;
    }

    h2 {
      border-left: 4px solid var(--water-mid);
      padding-left: 10px;
      margin-top: 40px;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 16px;
    }

    .card {
      border-radius: 12px;
      background: linear-gradient(145deg, #f4faff, #dceeff);
      border: 1px solid #b3dcff;
      padding: 16px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(0, 100, 255, 0.1);
    }

    footer {
      text-align: center;
      padding: 30px 10px;
      background: linear-gradient(180deg, var(--water-light), var(--water-mid));
      color: var(--text-light);
      margin-top: 40px;
      position: relative;
    }

    footer::after {
      content: "";
      position: absolute;
      top: -40px;
      left: 0;
      width: 100%;
      height: 40px;
      background: radial-gradient(ellipse at center, rgba(255,255,255,0.7) 0%, transparent 80%);
    }

    a {
      color: var(--water-dark);
    }
  </style>
</head>

<body>
  <header>
    <h1>Your Name</h1>
    <p>Flow like water — calm, steady, creative.</p>
    <nav>
      <a href="#projects">Projects</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </nav>
    <div class="wave"></div>
  </header>

  <main>
    <section id="projects">
      <h2>💧 Projects</h2>
      <div class="grid">
        <article class="card">
          <h3>Ripple Effect</h3>
          <p>A smooth animation project inspired by water surface ripples.</p>
          <a href="#">View project</a>
        </article>
        <article class="card">
          <h3>Tide Tracker</h3>
          <p>Real-time tide and surf condition web app using public APIs.</p>
          <a href="#">View project</a>
        </article>
        <article class="card">
          <h3>Coral UI</h3>
          <p>A minimalist, ocean-inspired CSS component library.</p>
          <a href="#">View project</a>
        </article>
      </div>
    </section>

    <section id="about">
      <h2>🌊 About Me</h2>
      <p>
        I’m a developer/designer who finds creativity in calmness and structure in flow.  
        Inspired by natural movement — water currents, waves, and reflection — my work focuses on intuitive, fluid digital experiences.
      </p>
    </section>

    <section id="contact">
      <h2>💦 Contact</h2>
      <p>
        📧 <a href="mailto:you@example.com">you@example.com</a><br>
        🌐 <a href="https://github.com/yourusername" target="_blank">GitHub</a> |
        <a href="https://linkedin.com/in/yourprofile" target="_blank">LinkedIn</a>
      </p>
    </section>
  </main>

  <footer>
    © <span id="year"></span> Your Name — Flowing Water Theme 🌊
  </footer>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
